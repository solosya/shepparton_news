{{ set(this, 'title', _Blog.getBlogTitle()) }}
{% set networkData = _Network.getNetworkData() %}
{% set blogData    = _Blog.getBlog() %}

{ "blogHeaderMenu": 
{{ blogData.blogHeaderMenu|json_encode(constant('JSON_PRETTY_PRINT')) }}
{ "blogFooterMenu": 
{{ blogData.blogFooterMenu|json_encode(constant('JSON_PRETTY_PRINT')) }}
}