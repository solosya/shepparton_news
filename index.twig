{{ set(this, 'title', _Blog.getBlogTitle()) }}
{% set networkData = _Network.getNetworkData() %}


{{this.registerJs("CardController();")}}

{% set limit = 30 %}

{% set articlesArr = _Blog.getBlogFeed({'limit': limit, 'offset': 0}) %}
{% set articles = articlesArr.articles %}


{% set events = _Event.getEvents() %}
{# <pre>
    {{dump(events)}}
</pre>

 #}<!-- used to index each article as we loop over throughout the template -->
{% set articleCount = 0  %}
{% set dev = _AppHelper.getApplicationEnv() == 'DEV' ? true : false %}


    {% set sections =  {
            "videos": {
                "feed": "",
                "link": "",
                "limit" : 10
            }, 
            "local news": {
                "feed": "",
                "link": "",
                "limit" : 10
            }, 
            "regional news": {
                "feed": "",
                "link": "",
                "limit" : 10
            },
            "opinion": {
                "feed": "",
                "link": "",
                "limit" : 10
            },
            "rural news": {
                "feed": "",
                "link": "",
                "limit" : 10
            },
            "national": {
                "feed": "",
                "link": "",
                "limit" : 10
            },
            "sport": {
                "feed": "",
                "link": "",
                "limit" : 10
            },
            "business": {
                "feed": "",
                "link": "",
                "limit" : 10
            },
            "bloggers": {
                "feed": "",
                "link": "",
                "limit" : 2
            }

        }
    %}

    {% for key, value in sections %}
        {% for blog in networkData.networkBlogs %}
            {% if blog.title | lower == key %}
                {% set sections = sections | merge({
                    (key) : {
                        'feed': _Blog.getBlogFeed({'limit': sections[key]['limit'], 'offset': 0, 'blogid':blog.guid}),
                        'link' : blog.url
                        }
                    }) %}
            {% endif %}
        {% endfor %}
    {% endfor %}











    <main id="main" class="site-main" role="main">
        
        <div class="container">

            <section class="home-main-stories">
                <div class="row">
                    <div class="col-xs-9 col-sm-6">

                        <div id="weather-mob" class="weather">
                            <div>
                                <p class="location">location</p>
                                <p class="description">Partly Cloudy</p>
                            </div>
                            <img class="icon" src="{{networkData.templatePath}}/static/icons/weather_partly_cloudy.svg">
                            <p class="temp">21&deg;</p>
                        </div>




                        {% if articles[articleCount] %}                                                                
                            {{this.render('partials/_single-article.twig', {
                                article: articles[articleCount], 
                                position: articleCount + 1, 
                                imageSize: {width:630, height:400},
                                behaviour: 'lightbox',
                                containerClass: 'home-feature home-feature-mobile'
                            }) | raw}}
                            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}

                        {% endif %}
                    </div>


                    {% if articles[articleCount] %}
                    <div class="cold-xs-9 col-sm-3">

                        <div id="weather-dt" class="weather">
                            <div>
                                <p class="location">location</p>
                                <p class="description">Partly Cloudy</p>
                            </div>
                            <img class="icon" src="{{networkData.templatePath}}/static/icons/weather_partly_cloudy.svg">
                            <p class="temp">21&deg;</p>
                        </div>



                        {% for i in 0..2 if articles[i] %}
                            {{this.render('partials/_single-article.twig', {
                                article: articles[articleCount], 
                                position: articleCount + 1, 
                                containerClass: 'feature-side'
                            }) | raw}}
                            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}

                        {% endfor %}
                    </div><!-- .row-->
                    {% endif %}

                </div>
            </section><!-- .featured-slider-->

{# 
            <div class="col-sm-12">
                 <section class="" style="">
                    <div class="row">
                        <div class="ajaxArticles index clearfix"
                             data-offset="{{limit}}"
                             data-articlecount="10"
                             data-containerclass="col-sm-4 card-sm"
                             data-existing-nonpinned-count="{{articlesArr.existingNonPinnedCount}}">
                         </div>
                        <a class="loadMoreArticles" href="#">Load more articles</a>
                    </div>

                    {% set noDataCls = 'hide' %}
                    {% if articles|length == 0 %}
                        {% set noDataCls = '' %}
                     {% endif %}
                    <div class="row text-center has-margin-bottom-20 has-margin-top-20 {{noDataCls}} NoArticlesMsg">
                        <p class="no-data text-center">No articles found.</p>  
                    </div>
                </section>
            </div>

 #}


            <section class="row five-cols top-stories-row">
                
                {% for i in 0..4 if articles[articleCount] %}
                    {{this.render('partials/_single-article.twig', {
                        article: articles[articleCount], 
                        position: 1, 
                        imageSize: {width:500},
                        containerClass: 'col-sm-1 top-stories top-stories-mobile'
                    }) | raw}}
                    {% set articleCount = (dev) ? articleCount : articleCount + 1 %}

                {% endfor %}
            </section>











            <section class="middle">
                <div class="row">
                    <div class="col-sm-2 most-popular">
                        <h2>Most popular</h2>

                        {% for i in 0..4 if articles[articleCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: articles[articleCount], 
                                position: 1,
                                imageSuppress: true, 
                                containerClass: 'card-popular'
                            }) | raw}}
                        {% endfor %}

                        <hr class="divide visible-xs-block">

                    </div>

                    <div class="col-sm-2">
                        {% if articles[articleCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: articles[articleCount], 
                                position: articleCount + 1, 
                                imageSize: {width:190},
                                containerClass: 'card-sm card-sm-mobile'
                            }) | raw}}
                            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
                            <hr class="divide visible-xs-block">

                        {% endif %}
                    </div>


                    <div class="col-sm-2">


                        {% if articles[articleCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: articles[articleCount], 
                                position: articleCount + 1, 
                                imageSize: {width:190},
                                containerClass: 'card-sm card-sm-mobile'
                            }) | raw}}
                            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}

                        {% endif %}
                    </div>


                    <div class="col-sm-3">
                        <div class="ad-square"></div>
                    </div>
                </div>
                    

                <div class="row row-space">
                    <div class="col-sm-3">
                        {% if articles[articleCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: articles[articleCount], 
                                position: articleCount + 1, 
                                imageSize: {width:300},
                                containerClass: 'card-sm-wide card-sm2-mobile'
                            }) | raw}}
                            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
                            
                            <hr class="divide visible-xs-block">

                        {% endif %}
                    </div>


                    <div class="col-sm-3">
                        {% if articles[articleCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: articles[articleCount], 
                                position: articleCount + 1, 
                                imageSize: {width:300},
                                containerClass: 'card-sm-wide card-sm2-mobile'
                            }) | raw}}
                            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
                            <hr class="divide visible-xs-block">

                        {% endif %}


                    </div>
                    <div class="col-sm-3">
                        {% for i in 0..1 if articles[articleCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: articles[articleCount], 
                                position: articleCount + 1, 
                                imageSize: {width:95},
                                containerClass: 'card-xs card-sm2-mobile'
                            }) | raw}}
                            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
                            

                            {% if i == 0 %}
                                <hr class="divide">
                            {% endif %}
                        {% endfor %}

                    </div>
                </div>

            </section>







            <section class="advert" style="background:gray;height:200px;"></section>

        </div>



        {% if sections['videos'].feed.articles|length > 0 %}
            {% set videoCount = 0 %}

            <section class="section-background">
                <div class="container">
                    <div class="row">
                        <div class="col-sm-9 section-header">
                            <p><a href="{{sections['videos'].link}}">View All</a></p>
                            <div class="hk">
                                <h2>VIDEOS</h2>
                                <p></p>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        {% for i in 0..2 if sections['videos'].feed.articles[videoCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: sections['videos'].feed.articles[videoCount], 
                                imageSize: {width:95},
                                containerClass: 'col-sm-3 card-sm-wide card-sm-wide-mobile'
                            }) | raw}}
                            {% set videoCount = (dev) ? videoCount : videoCount + 1 %}

                        {% endfor %}

                    </div>
                </div>
            </section>
        {% endif %}






        <div class="container">




            {% if sections['local news'].feed.articles|length > 0 %}
                {% set localCount = 0 %}

                <section class="row">
                    <div class="col-sm-9 section-header">
                        <p><a href="{{sections['local news'].link}}">View All</a></p>
                        <div class="hk">
                            <h2>LOCAL NEWS</h2>
                            <p></p>
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="row">
                            <div class="col-sm-9">
                                {% if sections['local news'].feed.articles[localCount] %}
                                    {{this.render('partials/_single-article.twig', {
                                        article: sections['local news'].feed.articles[localCount], 
                                        position: articleCount + 1, 
                                        imageSize: {width:630},
                                        containerClass: 'home-feature-sm home-feature-mobile'
                                    }) | raw}}
                                    {% set localCount = (dev) ? localCount : localCount + 1 %}
                                {% endif %}
                            </div>
                        </div>


                        <div class="row row-space">
                            <div class="col-sm-3 events">
                                <h2>Events</h2>

                                {% for i in 0..4 if events[0] %}
                                    {{this.render('partials/_single-event.twig', {
                                        article: events[0], 
                                        imageSize: {width: 60, height: 37},
                                        containerClass: 'card-popular'
                                    }) | raw}}
                                {% endfor %}
                            </div>



                            <div class="col-sm-6">
                                <div class="row">
                                    <div class="col-sm-9">
                                        {% if sections['local news'].feed.articles[localCount] %}
                                            {{this.render('partials/_single-article.twig', {
                                                article: sections['local news'].feed.articles[localCount], 
                                                imageSize: {width:630},
                                                containerClass: 'card-rec-full'
                                            }) | raw}}
                                            {% set localCount = (dev) ? localCount : localCount + 1 %}
                                            <hr class="divide visible-xs-block">
                                        {% endif %}

                                    </div>
                                </div>

                                <div class="row two-cols row-space">
                                    {% for i in 0..1 if sections['local news'].feed.articles[localCount] %}
                                        {{this.render('partials/_single-article.twig', {
                                            article: sections['local news'].feed.articles[localCount], 
                                            imageSize: {width:188},
                                            containerClass: 'col-sm-1 card-sm card-sm-mobile'
                                        }) | raw}}
                                        {% set localCount = (dev) ? localCount : localCount + 1 %}
                                        <hr class="divide visible-xs-block">

                                    {% endfor %}
                                </div>

                            </div>

                        </div>
                    </div>
                    <div class="col-sm-3">
                        
                        {% for i in 0..1 if sections['local news'].feed.articles[localCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: sections['local news'].feed.articles[localCount], 
                                imageSuppress: true,
                                containerClass: 'card-xs card-xs-mobile'
                            }) | raw}}
                            {% set localCount = (dev) ? localCount : localCount + 1 %}
                           
                            {% if i == 0 %}
                                <hr class="divide">
                            {% endif %}

                        {% endfor %}
                
                        <div class="ad-large-vert" style="margin-top:30px;"></div>
                    </div>

                </section>
            {% endif %}







            {% if sections['bloggers'].feed.articles|length > 0 %}

                <section class="row">
                    <div class="col-sm-3">
                        <img src="{{networkData.templatePath}}/static/images/bloggers.svg">
                    </div>


                    {% for i in 0..1 if sections['bloggers'].feed.articles[i] %}
                        {{this.render('partials/_single-article.twig', {
                            article: sections['bloggers'].feed.articles[i], 
                            imageSuppress: true,
                            containerClass: 'col-sm-3 card-blogger card-blogger-mobile'
                        }) | raw}}

                        {% if i == 0 %}
                            <hr class="divide visible-xs-block">
                        {% endif %}

                    {% endfor %}

                </section>
            {% endif %}







            {% if sections['regional news'].feed.articles|length > 0 %}
                {% set regionalCount = 0 %}

                <section class="row">
                    <div class="col-sm-9 section-header">
                        <p><a href="{{sections['regional news'].link}}">View All</a></p>
                        <div class="hk">
                            <h2>REGIONAL NEWS</h2>
                            <p></p>
                        </div>
                    </div>

                    <div class="col-sm-4">
                        {% if sections['regional news'].feed.articles[regionalCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: sections['regional news'].feed.articles[regionalCount], 
                                imageSize: {width:409, height:530},
                                containerClass: 'home-feature-thin home-feature-thin-mobile'
                            }) | raw}}
                            {% set regionalCount = (dev) ? regionalCount : regionalCount + 1 %}
                        {% endif %}



                    </div>
                    <div class="col-sm-2">
                        {% set cardspace = ['', 'row-space'] %}
                        {% for i in 0..1 if sections['regional news'].feed.articles[regionalCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: sections['regional news'].feed.articles[regionalCount], 
                                imageSize: {width:190},
                                containerClass: 'card-sm card-sm-mobile ' ~ cardspace[i]
                            }) | raw}}
                            {% set regionalCount = (dev) ? regionalCount : regionalCount + 1 %}
                            <hr class="divide visible-xs-block">

                        {% endfor %}

                    </div>


                    <div class="col-sm-3">
                        
                        {% for i in 0..1 if sections['regional news'].feed.articles[regionalCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: sections['regional news'].feed.articles[regionalCount], 
                                imageSuppress: true,
                                containerClass: 'card-xs card-xs-mobile'
                            }) | raw}}
                            {% set regionalCount = (dev) ? regionalCount : regionalCount + 1 %}
                            {% if i == 0 %}
                                <hr class="divide">
                            {% endif %}

                        {% endfor %}


                        <div class="ad-square" style="margin-top:30px;"></div>

                    </div>
                </section>
            {% endif %}









{# 
            <section class="row">
                <div class="col-sm-9 section-header">
                    <p>View All</p>
                    <div class="hk">
                        <h2>HEADER 4</h2>
                        <p></p>
                    </div>
                </div>
                <div class="col-sm-2" style="background:red;height:100px;"></div>
                <div class="col-sm-2" style="background:green;height:100px;"></div>
                <div class="col-sm-2" style="background:orange;height:100px;"></div>
                <div class="col-sm-3" style="background:pink;height:100px;"></div>
            </section>


 #}






            {% if sections['rural news'].feed.articles|length > 0 %}
                {% set ruralCount = 0 %}

                <section>
                    <div class="row">
                        <div class="col-sm-9 section-header">
                            <p><a href="{{sections['rural news'].link}}">View All</a></p>
                            <div class="hk">
                                <h2>RURAL NEWS</h2>
                                <p></p>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        {% for i in 0..2 if sections['rural news'].feed.articles[ruralCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: sections['rural news'].feed.articles[ruralCount], 
                                imageSize: {width:95},
                                containerClass: 'col-sm-3 card-sm-wide card-sm2-mobile'
                            }) | raw}}
                            {% set ruralCount = (dev) ? ruralCount : ruralCount + 1 %}
                            {% if i < 2 %}
                                <hr class="divide visible-xs-block">
                            {% endif %}
                        {% endfor %}

                    </div>
                </section>
            {% endif %}










            {% if sections['national'].feed.articles|length > 0 %}
                {% set nationalCount = 0 %}

                <section class="row">
                    <div class="col-sm-9 section-header">
                        <p><a href="{{sections['national'].link}}">View All</a></p>
                        <div class="hk">
                            <h2>NATIONAL</h2>
                            <p></p>
                        </div>
                    </div>

                    {% for i in 0..2 if sections['national'].feed.articles[nationalCount] %}
                        {{this.render('partials/_single-article.twig', {
                            article: sections['national'].feed.articles[nationalCount], 
                            imageSize: {width:95},
                            containerClass: 'col-sm-2 card-sm card-sm-mobile'
                        }) | raw}}
                        {% set nationalCount = (dev) ? nationalCount : nationalCount + 1 %}
                        <hr class="divide visible-xs-block" style="margin-left:15px;margin-right:15px;">
                    {% endfor %}

                    {% if sections['national'].feed.articles[nationalCount] %}
                        {{this.render('partials/_single-article.twig', {
                            article: sections['national'].feed.articles[nationalCount], 
                            imageSize: {width:95},
                            containerClass: 'col-sm-3 card-sm-wide card-sm2-mobile'
                        }) | raw}}
                        {% set nationalCount = (dev) ? nationalCount : nationalCount + 1 %}

                    {% endif %}
                </section>
            {% endif %}










            {% if sections['sport'].feed.articles|length > 0 %}
                {% set sportCount = 0 %}

                <section>
                    <div class="row">
                        <div class="col-sm-9 section-header">
                            <p><a href="{{sections['sport'].link}}">View All</a></p>
                            <div class="hk">
                                <h2>SPORT</h2>
                                <p></p>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-sm-6">
                           {% if sections['sport'].feed.articles[sportCount] %}
                               {{this.render('partials/_single-article.twig', {
                                   article: sections['sport'].feed.articles[sportCount], 
                                   imageSize: {width:630},
                                   containerClass: 'home-feature-sm home-feature-mobile'
                               }) | raw}}
                               {% set sportCount = (dev) ? sportCount : sportCount + 1 %}
                           {% endif %}
     

                        </div>
                        <div class="col-sm-3">
                        
                            <div class="ad-square" style="margin-bottom:30px;"></div>

                             {% if sections['sport'].feed.articles[sportCount] %}
                                 {{this.render('partials/_single-article.twig', {
                                     article: sections['sport'].feed.articles[sportCount], 
                                     imageSize: {width:80},
                                     containerClass: 'card-xs card-xs-mobile'
                                 }) | raw}}

                                <hr class="divide visible-xs-block">

                                {% set sportCount = (dev) ? sportCount : sportCount + 1 %}
                             {% endif %}
                   
        

                        </div>
                    </div>

                    <div class="row row-space">

                        {% for i in 0..2 if sections['sport'].feed.articles[sportCount] %}
                            {{this.render('partials/_single-article.twig', {
                                article: sections['sport'].feed.articles[sportCount], 
                                imageSize: {width:80},
                                containerClass: 'col-sm-3 card-xs card-xs-mobile divideright'
                            }) | raw}}
                            {% set sportCount = (dev) ? sportCount : sportCount + 1 %}
                            {% if i < 2 %}
                                <hr class="divide visible-xs-block">
                            {% endif %}
                        {% endfor %}

                    </div>
                </section>
            {% endif %}







            {% if sections['business'].feed.articles|length > 0 %}
                {% set businessCount = 0 %}

                <section class="row">
                     <div class="col-sm-9 section-header">
                        <p><a href="{{sections['business'].link}}">View All</a></p>
                         <div class="hk">
                             <h2>BUSINESS</h2>
                             <p></p>
                         </div>
                     </div>

                     <div class="col-sm-4">
                         {% if sections['business'].feed.articles[businessCount] %}
                             {{this.render('partials/_single-article.twig', {
                                 article: sections['business'].feed.articles[businessCount], 
                                 imageSize: {width:630},
                                 containerClass: 'home-feature-thin home-feature-thin-mobile'
                             }) | raw}}
                             {% set businessCount = (dev) ? businessCount : businessCount + 1 %}
                         {% endif %}



                     </div>
                     <div class="col-sm-2">
                         {% set cardspace = ['', 'row-space'] %}

                         {% for i in 0..1 if sections['business'].feed.articles[businessCount] %}
                             {{this.render('partials/_single-article.twig', {
                                 article: sections['business'].feed.articles[businessCount], 
                                 imageSize: {width:190},
                                 containerClass: 'card-sm card-sm-mobile ' ~ cardspace[i]
                             }) | raw}}
                             {% set businessCount = (dev) ? businessCount : businessCount + 1 %}
                             <hr class="divide visible-xs-block">

                         {% endfor %}



                     </div>
                     <div class="col-sm-3">
                         <div class="ad-square" style="margin-bottom:30px;"></div>

                         {% for i in 0..1 if sections['business'].feed.articles[businessCount] %}
                             {{this.render('partials/_single-article.twig', {
                                 article: sections['business'].feed.articles[businessCount], 
                                 imageSize: {width:80},
                                 containerClass: 'card-xs card-xs-mobile'
                             }) | raw}}
                             {% set businessCount = (dev) ? businessCount : businessCount + 1 %}
                             {% if i == 0 %}
                                 <hr class="divide">
                             {% endif %}

                         {% endfor %}



                     </div>
                </section>
            {% endif %}






        </div>


    </main><!-- .site-main -->