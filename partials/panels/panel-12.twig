{% set articleCount = 0 %}




<div class="col-sm-3" data-panel="panel-12">
    <div class="row u-margin-bottom-30">
        <div class="col-sm-12">
            <div class="c-section-header  u-margin-top-30">
                <a class="{{site}}" href="{{link}}">
                    <h2 class="c-section-header__h2 {{site}}">{{title}}</h2>
                </a>
            </div>
        </div>
    </div>
    <div class="row ">

        {% for i in 0..1 if articles[articleCount] %}
            {{this.render('../_single-article.twig', {
                article         : articles[articleCount], 
                imageSize       : {width:300, height:160},
                site            : site,
                containerClass  : 'col-sm-6 card-110-mobile card-110-tablet card-110-desktop-sm card-110-desktop-lg'
            }) | raw}}
            {% set articleCount = (dev) ? articleCount : articleCount + 1 %}
            
        {% endfor %}

    </div>

</div>
